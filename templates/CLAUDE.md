@AGENTS.md

The file imported above is the whole instruction set for this repository. This file exists
only because this product reads `CLAUDE.md` and does not read `AGENTS.md` on its own; it
carries no rule of its own, because a rule copied here is a second copy to keep in step, and
the copy is the one that goes stale.
